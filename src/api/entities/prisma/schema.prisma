// schema.prisma

model Country {
  id          String    @id @default(uuid()) @map("id")
  name String

  @@map("country") 
}


datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Generate Prisma client
generator client {
  provider = "prisma-client-js"
}
